test: 8888888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info # Endpoints to expose
  endpoint:
    refresh:
      enabled: true
    health:
      show-details: always
